
    <section>
        <header>
            <h1>{{{title}}}</h1>
            <time datetime="{{w3c_date}}">{{date}}</time>
        </header>
        <div class="post-content">
            {{{content}}}
        </div>
    </section>
